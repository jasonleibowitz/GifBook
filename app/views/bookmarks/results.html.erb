<h2>Giphy Search Results</h2>

<%= @giphy_urls_ary.each do |giphy| %>
  <div>
    <img src="<%= giphy %>" alt="">
    <% session[:giphy_url] = giphy %><br />
    <%= link_to("Bookmark this GIF", new_user_bookmark_path(current_user)) %>
  </div>
<% end %>
